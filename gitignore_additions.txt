# ── Tribal Fire Science — .gitignore additions ────────────────────────────────
# Merge these into your existing .gitignore

# Data — never commit raw or cached data to the public repo
data/raw/
data/interim/
data/cache/
data/final/

# Keep the directory structure
!data/raw/.gitkeep
!data/interim/.gitkeep
!data/cache/.gitkeep
!data/final/.gitkeep

# API keys / secrets (use .env, never commit)
.env
*.env
secrets.yml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
*.ipynb_checkpoints

# Outputs — large figures/exports
outputs/*.png
outputs/*.pdf
outputs/*.html

# Conda env
*.conda

# VSCode
.vscode/settings.json
